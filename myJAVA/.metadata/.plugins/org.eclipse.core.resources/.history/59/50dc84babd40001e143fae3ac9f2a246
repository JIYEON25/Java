package jy.java.exam09;

public class Sample {
	// 멤버필드
	private int x;
	int y;
	// 생성자 - default 생성자
	// public Sample(){ }
	// 메서드
}
/* 
 * private - 클래스 내부에서만
 * default(안쓰면) - 클래스내부 + 같은 패키지
 * protected - 클래스 내부 + 같은 패키지 + 상속관계
 * public - 누구나 
 */
